name: Main

on: [push, pull_request]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Gen
      run: yarn gen
    - name: Build WASM
      run: yarn build
    - name: Webpack
      run: yarn webpack
