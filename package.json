{
  "name": "utf",
  "version": "1.0.0",
  "description": "Display information about unicode characters",
  "author": "Alex Macleod <alex@macleod.io>",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Alexendoo/utf.git"
  },
  "dependencies": {
    "prettier": "^1.13.5",
    "typescript": "~2.9.2"
  },
  "scripts": {
    "build": "cargo build --target wasm32-unknown-unknown --lib --release -Z unstable-options --out-dir src/wasm",
    "format": "prettier --write \"src/**/*.{css,json,ts,js}\"",
    "generate": "cargo run --bin gen --release --features gen"
  }
}
